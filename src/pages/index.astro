---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const posts = await getCollection('posts');

// Group posts by category
const categoryMap = new Map<string, typeof posts>();
posts.forEach(post => {
  const category = post.data.categories || '未分类';
  if (!categoryMap.has(category)) {
    categoryMap.set(category, []);
  }
  categoryMap.get(category)!.push(post);
});

// Convert to array and sort by post count
const categories = Array.from(categoryMap.entries())
  .map(([name, posts]) => ({ name, count: posts.length }))
  .sort((a, b) => b.count - a.count);

// Category icons/colors
const categoryStyles: Record<string, { icon: string; gradient: string }> = {
  '网络安全': { icon: '🛡️', gradient: 'linear-gradient(135deg, #f97316, #ef4444)' },
  'WSL相关问题': { icon: '🖥️', gradient: 'linear-gradient(135deg, #8b5cf6, #6366f1)' },
  '线性代数': { icon: '📐', gradient: 'linear-gradient(135deg, #10b981, #14b8a6)' },
  '初学hexo': { icon: '📝', gradient: 'linear-gradient(135deg, #3b82f6, #06b6d4)' },
  '未分类': { icon: '📁', gradient: 'linear-gradient(135deg, #6b7280, #9ca3af)' },
};
---

<BaseLayout title="WJ Blog - 首页">
  <div class="container">
    <section class="hero">
      <h1 class="hero-title fade-in">欢迎来到我的技术博客</h1>
      <p class="hero-description slide-in">分享编程、WSL、线性代数等技术文章</p>
    </section>

    <section class="categories-section">
      <h2 class="section-title">📚 文章分类</h2>
      <div class="categories-grid">
        {categories.map(({ name, count }) => {
          const style = categoryStyles[name] || categoryStyles['未分类'];
          return (
            <a href={`/categories/${name}`} class="category-card">
              <div class="category-icon" style={`background: ${style.gradient}`}>
                {style.icon}
              </div>
              <div class="category-info">
                <h3 class="category-name">{name}</h3>
                <p class="category-count">{count} 篇文章</p>
              </div>
              <div class="category-arrow">→</div>
            </a>
          );
        })}
      </div>
    </section>

    <section class="recent-section">
      <h2 class="section-title">🕐 最新文章</h2>
      <div class="recent-posts">
        {posts
          .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
          .slice(0, 3)
          .map(post => (
            <a href={`/posts/${post.slug}`} class="recent-post">
              <span class="recent-title">{post.data.title}</span>
              <span class="recent-date">
                {new Intl.DateTimeFormat('zh-CN', {
                  month: 'short',
                  day: 'numeric'
                }).format(post.data.date)}
              </span>
            </a>
          ))}
      </div>
    </section>
  </div>
</BaseLayout>

<style>
  .hero {
    text-align: center;
    padding: var(--space-2xl) 0;
    margin-bottom: var(--space-xl);
  }

  .hero-title {
    font-size: 3rem;
    margin-bottom: var(--space-md);
  }

  .hero-description {
    font-size: 1.25rem;
    color: var(--color-text-secondary);
    max-width: 600px;
    margin: 0 auto;
  }

  .section-title {
    font-size: 1.5rem;
    margin-bottom: var(--space-lg);
    color: var(--color-text);
  }

  .categories-section {
    margin-bottom: var(--space-2xl);
  }

  .categories-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: var(--space-lg);
  }

  .category-card {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    padding: var(--space-lg);
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    text-decoration: none;
    color: inherit;
    transition: all 0.3s ease;
    box-shadow: var(--shadow-sm);
  }

  .category-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
    border-color: var(--color-primary);
  }

  .category-icon {
    width: 56px;
    height: 56px;
    border-radius: var(--radius-md);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    flex-shrink: 0;
  }

  .category-info {
    flex: 1;
  }

  .category-name {
    font-size: 1.1rem;
    font-weight: 600;
    margin-bottom: 4px;
    color: var(--color-text);
  }

  .category-count {
    font-size: 0.9rem;
    color: var(--color-text-secondary);
    margin: 0;
  }

  .category-arrow {
    font-size: 1.25rem;
    color: var(--color-text-secondary);
    opacity: 0;
    transform: translateX(-8px);
    transition: all 0.3s ease;
  }

  .category-card:hover .category-arrow {
    opacity: 1;
    transform: translateX(0);
    color: var(--color-primary);
  }

  .recent-section {
    margin-bottom: var(--space-2xl);
  }

  .recent-posts {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
    max-width: 600px;
  }

  .recent-post {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: var(--space-md) var(--space-lg);
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    text-decoration: none;
    color: inherit;
    transition: all 0.2s ease;
  }

  .recent-post:hover {
    border-color: var(--color-primary);
    background: rgba(99, 102, 241, 0.05);
  }

  .recent-title {
    font-weight: 500;
    color: var(--color-text);
  }

  .recent-date {
    font-size: 0.85rem;
    color: var(--color-text-secondary);
  }

  @media (max-width: 768px) {
    .hero-title {
      font-size: 2rem;
    }

    .hero-description {
      font-size: 1rem;
    }

    .categories-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
